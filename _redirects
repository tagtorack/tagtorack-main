# Cloudflare Pages redirects file
# Reference: https://developers.cloudflare.com/pages/configuration/redirects/
#
# Format: <source>  <destination>  <status>
# Status 301 = permanent. Use 302 only for temporary.
#
# The app is served entirely on the apex tagtorack.com (there is no submit. subdomain):
#   sellers land at /m/<slug>, generic entry at /submit, merchant portal at /portal,
#   operator dashboard at /admin. These are served by Pages Functions directly,
#   so no host redirects are needed.
